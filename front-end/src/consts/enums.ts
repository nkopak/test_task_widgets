export enum PathEnum {
  main = "/",
  products = "/products",
  reviews = "/reviews",
}

export enum ReviewFormFields {
  authorName = "authorName",
  date = "date",
  rating = "rating",
  comment = "comment",
  email = "email",
  phone = "phone",
  saveUserInfo = "saveUserInfo",
}
